extends layout-admin

block content
    h1 Админка
    
    table
        thead
            th Id
            th Название
            th Курсы
            th Загружены
            th Ошиб. курсы
            th Сумма резервов
            th Расчёт
            th Дата начала
            th 
            th 
            th 
            th 
        tbody
        each ex in exchList
            <tr>
                td #{ex.id}
                td #{ex.name}
                td =
                td #{ex.xmlStartedAt && new Date(ex.xmlStartedAt)}
                td err?
                td =
                <!--td>Обменник: #{JSON.stringify(exch)}</td-->
                td ?
                td #{ex.createdAt && formatDate(new Date(ex.createdAt))}
                td 
                td 
                td 
                td 
                td 
            </tr>
