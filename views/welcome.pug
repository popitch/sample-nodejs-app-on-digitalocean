extends layout

block content
    h1 Здравствуй, #{ user ? user.login : 'Анонимаус' }!

    if (user)
        p Админка #[a(href="/admin/table/exchangers") здесь], через несколько секунд мы отправимся...
        
        - var code = "setTimeout(_ => { location = '/admin/table/exchangers' }, 1500)";
        script !{code}
    else
        p Логин #[a(href="/login") здесь]
