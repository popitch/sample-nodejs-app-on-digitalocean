extends layout

block content
    h1 Здравствуй, #{ user ? user.login : 'Анонимаус' }!

    if (user)
        p Админка #[a(href="/admin/index") здесь]
    else
        p Логин #[a(href="/login") здесь]
