form#exchange-form
    .column
        h2 Отдаю
        .line
            select(name="from", value="BTC")
            option(value="BTC") Bitcoin (BTC)
        .line
            input(name="in", value="0.02")
    .column
        h2 Получаю
        .line
            select(name="to", value="SBERRUB")
                option(value="SBERRUB") Sberbank RUB (SBERRUB)
        .line
            input(name="out", value="50000")
