extends layout

append css
    link(rel='stylesheet', href="/css/exchange-pair.css", media="all")

block content
    h1= title
    
    include includes/exch-form.pug
    br
    include includes/exch-table.pug

append scripts
    script(type="html/template" id="number-currency.template")
        strong(data-bind="text: value")
        small(data-bind="text: CURRENCY_NAME_BY_SYMBOL[symbol] || symbol")
        
    script(src="/js/table.js?sniffer&abc=def&gh=&ijkl")
